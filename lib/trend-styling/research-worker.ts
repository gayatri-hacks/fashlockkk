import { createHash } from "crypto";
import { SUPPORTED_TREND_REGIONS } from "@/lib/trends/config";
import { getSupabaseClient } from "@/lib/supabase";
import { deduplicateStylingSources } from "./evidence-deduplication";
import type { PublicEvidenceResult, StylingEvidenceSearchProvider } from "./evidence-provider";
import { MARKET_RESEARCH_LANGUAGES, selectStylingResearchMarkets, type StylingMarketSignal } from "./market-selection";
import { isolatedConceptId } from "./concept-identity";
import {
  createRepoOwnedMarketInterestProvider,
  MARKET_DISCOVERY_LIMITS,
  MARKET_DISCOVERY_PROVIDER_NAME,
  parseRepoMarketDiscoveryBatch,
  type MarketDiscoveryBatch,
  type MarketInterestProvider,
} from "./market-discovery-provider";

export const RESEARCH_LIMITS = { maxKeywordsPerRun: 1, maxMarketDiscoveryProviderCallsPerKeyword: MARKET_DISCOVERY_LIMITS.maxProviderCallsPerKeyword, maxStylingSearchCallsPerKeyword: 16, maxResultsPerCall: 6 } as const;
export type ResearchJob = { id: string; concept_id: string; canonical_keyword: string; requesting_market: string; season: string; attempts: number; max_attempts: number };
export type EvidenceInsert = { id:string; concept_id: string; canonical_keyword: string; audience: "women"|"men"; region: string; season: string; garment_pairings: string[]; silhouettes: string[]; materials: string[]; colours: string[]; footwear: string[]; accessories: string[]; styling_techniques: string[]; source_url: string; source_domain: string; source_language: string; short_extract: string; published_at: string; observed_at: string; quality_score: number; recency_score: number; market_relevance_score: number; content_fingerprint: string };
export interface ResearchStore { claim(workerId: string): Promise<ResearchJob|null>; loadMarketEvidence?(job:ResearchJob,now:Date):Promise<MarketDiscoveryBatch|null>; saveMarketEvidence?(job:ResearchJob,batch:MarketDiscoveryBatch,now:Date):Promise<void>; insertEvidence(rows: EvidenceInsert[]): Promise<void>; complete(job: ResearchJob, data: { selectedMarkets: string[]; evaluatedMarkets: string[]; selectedReasons:Record<string,string[]>; evidenceHash: string }): Promise<void>; retry(job: ResearchJob, error: string, retryAfter?:string|null): Promise<void>; }

class InsufficientMarketDiscoveryError extends Error { constructor(readonly retryAfter:string){super("insufficient materially supported market evidence");this.name="InsufficientMarketDiscoveryError";} }
export function nextMarketDiscoveryRetry(batch:MarketDiscoveryBatch,now:Date){const candidates=batch.markets.map((market)=>market.retryInformation.nextRetryAt).filter((value):value is string=>Boolean(value)).map((value)=>new Date(value).getTime()).filter((value)=>Number.isFinite(value)&&value>now.getTime());const target=candidates.length?Math.min(...candidates):now.getTime()+6*3600000;return new Date(Math.max(now.getTime()+5*60000,Math.min(now.getTime()+24*3600000,target))).toISOString();}

const VOCAB = { garments: ["shirt","blouse","t-shirt","tee","jeans","trousers","skirt","dress","blazer","jacket","kurta","saree","shorts","waistcoat"], silhouettes: ["oversized","fitted","wide leg","barrel","cropped","relaxed","tailored","flared","straight leg"], materials: ["linen","cotton","denim","silk","satin","leather","suede","knit","crochet","wool"], colours: ["white","black","blue","indigo","cream","beige","brown","red","green","yellow","pink","navy"], footwear: ["loafers","sneakers","sandals","boots","heels","flats","mules"], accessories: ["belt","bag","scarf","necklace","earrings","watch","cap"], techniques: ["tucked","layered","belted","tonal","colour blocking","rolled sleeves","open shirt","high waist"] } as const;
function matches(text: string, values: readonly string[]) { const lower=` ${text.toLowerCase()} `; return values.filter((value)=>lower.includes(value)); }
export function extractStylingAttributes(text: string) { return { garment_pairings: matches(text,VOCAB.garments), silhouettes: matches(text,VOCAB.silhouettes), materials: matches(text,VOCAB.materials), colours: matches(text,VOCAB.colours), footwear: matches(text,VOCAB.footwear), accessories: matches(text,VOCAB.accessories), styling_techniques: matches(text,VOCAB.techniques) }; }
export function canonicalizeEvidenceUrl(value: string) { const url=new URL(value); url.hash=""; [...url.searchParams.keys()].forEach((key)=>{ if (/^(utm_|fbclid|gclid)/i.test(key)) url.searchParams.delete(key); }); url.hostname=url.hostname.toLowerCase().replace(/^www\./,""); url.pathname=url.pathname.replace(/\/$/,"")||"/"; return url.toString(); }
function freshness(date: string, now: Date) { const days=Math.max(0,(now.getTime()-new Date(date).getTime())/86400000); return days<=60?1:days<=120 ? .55 : 0; }
function quality(domain: string) { return /vogue|gq|elle|harpersbazaar|designer|retailer/i.test(domain)?.9:.65; }
export function evidenceFromResult(input: { result: PublicEvidenceResult; job: ResearchJob; audience: "women"|"men"; market: string; language: string; observedAt: Date }): EvidenceInsert|null { const url=canonicalizeEvidenceUrl(input.result.url); const published=input.result.publishedAt ? new Date(input.result.publishedAt) : input.observedAt; if(Number.isNaN(published.getTime())) return null; const attrs=extractStylingAttributes(`${input.result.title} ${input.result.shortExtract}`); if(attrs.garment_pairings.length<2 || !attrs.materials.length) return null; const source_domain=new URL(url).hostname; const content_fingerprint=createHash("sha256").update(`${input.result.title.toLowerCase()}|${input.result.shortExtract.toLowerCase()}`).digest("hex"); return { id:isolatedConceptId(`evidence ${input.job.concept_id} ${content_fingerprint}`), concept_id:input.job.concept_id, canonical_keyword:input.job.canonical_keyword, audience:input.audience, region:input.market, season:input.job.season, ...attrs, source_url:url, source_domain, source_language:input.language, short_extract:input.result.shortExtract.slice(0,500), published_at:published.toISOString(), observed_at:input.observedAt.toISOString(), quality_score:quality(source_domain), recency_score:freshness(published.toISOString(),input.observedAt), market_relevance_score:input.result.market===input.market?1:.6, content_fingerprint }; }

export async function runResearchWorker(input: { workerId: string; store: ResearchStore; interestProvider: MarketInterestProvider; searchProvider: StylingEvidenceSearchProvider; now?: Date }) { const job=await input.store.claim(input.workerId); if(!job) return {status:"idle" as const}; const now=input.now||new Date(); try { let discovery=await input.store.loadMarketEvidence?.(job,now)||null;if(!discovery){discovery=await input.interestProvider.discover({keyword:job.canonical_keyword,conceptId:job.concept_id});if(discovery.providerCalls>RESEARCH_LIMITS.maxMarketDiscoveryProviderCallsPerKeyword)throw new Error("market discovery provider-call limit exceeded");await input.store.saveMarketEvidence?.(job,discovery,now);} const signals:StylingMarketSignal[]=discovery.markets.map((score)=>({region:score.market,regionalMomentum:score.recentMomentum,currentInterestPercentile:score.normalizedInterest,confidence:score.confidence,dataFreshness:score.observationCompleteness,observationCount:Math.round(score.observationCompleteness*12)})); const plan=selectStylingResearchMarkets({signals,lifecycle:"RISING",requestingMarket:job.requesting_market}); if(plan.strongestMarkets.length<3)throw new InsufficientMarketDiscoveryError(nextMarketDiscoveryRetry(discovery,now)); const rows:EvidenceInsert[]=[]; let calls=0; for(const market of plan.researchMarkets) for(const language of MARKET_RESEARCH_LANGUAGES[market]) for(const audience of ["women","men"] as const){ if(++calls>RESEARCH_LIMITS.maxStylingSearchCallsPerKeyword) throw new Error("styling search call limit exceeded"); const raw=await input.searchProvider.search({keyword:job.canonical_keyword,audience,region:market,season:job.season,language}); for(const result of deduplicateStylingSources(raw.slice(0,RESEARCH_LIMITS.maxResultsPerCall))){ const row=evidenceFromResult({result,job,audience,market,language,observedAt:now}); if(row&&row.recency_score>0) rows.push(row); } } const unique=[...new Map(rows.map((row)=>[row.content_fingerprint,row])).values()]; if(new Set(unique.map((row)=>row.source_domain)).size<2) throw new Error("insufficient independent fresh evidence"); await input.store.insertEvidence(unique); const evidenceHash=createHash("sha256").update(JSON.stringify(unique.map((row)=>[row.content_fingerprint,row.region,row.audience,row.published_at]).sort())).digest("hex"); await input.store.complete(job,{selectedMarkets:plan.researchMarkets,evaluatedMarkets:plan.evaluatedMarkets,selectedReasons:plan.selectedReasons,evidenceHash}); return {status:"completed" as const,job,evidence:unique,marketPlan:plan,marketDiscovery:discovery}; } catch(error){ const seconds=Number((error as {retryAfterSeconds?:unknown})?.retryAfterSeconds);const providerRetry=Number.isFinite(seconds)&&seconds>0?new Date(now.getTime()+Math.min(86_400,Math.max(60,seconds))*1000).toISOString():null;const retryAfter=error instanceof InsufficientMarketDiscoveryError?error.retryAfter:providerRetry;await input.store.retry(job,error instanceof Error?error.message:String(error),retryAfter); return {status:"retryable" as const,job,error:error instanceof Error?error.message:String(error),retryAfter}; } }

export function createSupabaseResearchStore(options: { exactJobId?: string } = {}):ResearchStore { const db=getSupabaseClient(); if(!db) throw new Error("Supabase service credentials required"); return { async claim(workerId){if(options.exactJobId){const now=new Date();const {data:candidate,error:readError}=await db.from("trend_style_research_jobs").select("*").eq("id",options.exactJobId).maybeSingle();if(readError)throw readError;if(!candidate||candidate.status!=="pending"||candidate.attempts>=candidate.max_attempts)return null;if(candidate.retry_after&&new Date(candidate.retry_after)>now)return null;const {data,error}=await db.from("trend_style_research_jobs").update({status:"researching",attempts:candidate.attempts+1,updated_at:now.toISOString(),error_message:null,retry_after:null}).eq("id",options.exactJobId).eq("status","pending").eq("attempts",candidate.attempts).select("*").maybeSingle();if(error)throw error;return data||null;}const {data,error}=await db.rpc("claim_next_trend_style_research_job",{worker_id:workerId}); if(error)throw error; return data?.[0]||null;},async loadMarketEvidence(job,now){const {data,error}=await db.from("trend_style_market_evidence").select("market,normalized_interest,recent_momentum,confidence,observation_completeness,provider_timestamp,retry_information,failure_reason").eq("concept_id",job.concept_id).eq("provider",MARKET_DISCOVERY_PROVIDER_NAME).gt("expires_at",now.toISOString()).order("provider_timestamp",{ascending:false});if(error)throw error;const latest=new Map<string,any>();for(const row of data||[])if(!latest.has(row.market))latest.set(row.market,row);if(latest.size!==SUPPORTED_TREND_REGIONS.length)return null;return parseRepoMarketDiscoveryBatch({schemaVersion:"repo-pytrends-market-discovery-v1",provider:MARKET_DISCOVERY_PROVIDER_NAME,canonicalKeyword:job.canonical_keyword,generatedAt:[...latest.values()][0].provider_timestamp,cacheHit:true,providerCalls:0,maximumProviderCalls:RESEARCH_LIMITS.maxMarketDiscoveryProviderCallsPerKeyword,markets:SUPPORTED_TREND_REGIONS.map(({code})=>{const row=latest.get(code);return {market:code,normalizedInterest:row.normalized_interest,recentMomentum:row.recent_momentum,confidence:row.confidence,observationCompleteness:row.observation_completeness,providerTimestamp:row.provider_timestamp,retryInformation:row.retry_information,failureReason:row.failure_reason};})},job.canonical_keyword);},async saveMarketEvidence(job,batch,now){const hasFailure=batch.markets.some((market)=>market.failureReason);const expiresAt=hasFailure?nextMarketDiscoveryRetry(batch,now):new Date(now.getTime()+MARKET_DISCOVERY_LIMITS.cacheTtlHours*3600000).toISOString();const rows=batch.markets.map((market)=>({concept_id:job.concept_id,canonical_keyword:job.canonical_keyword,market:market.market,normalized_interest:market.normalizedInterest,recent_momentum:market.recentMomentum,confidence:market.confidence,observation_completeness:market.observationCompleteness,provider:batch.provider,provider_timestamp:market.providerTimestamp,retry_information:market.retryInformation,failure_reason:market.failureReason,expires_at:expiresAt,updated_at:now.toISOString()}));const {error}=await db.from("trend_style_market_evidence").upsert(rows,{onConflict:"concept_id,market,provider"});if(error)throw error;}, async insertEvidence(rows){const {error}=await db.from("trend_style_evidence").insert(rows);if(error)throw error;}, async complete(job,data){const {error}=await db.from("trend_style_research_jobs").update({status:"completed",selected_markets:data.selectedMarkets,evaluated_markets:data.evaluatedMarkets,selected_market_reasons:data.selectedReasons,evidence_hash:data.evidenceHash,retry_after:null,completed_at:new Date().toISOString()}).eq("id",job.id);if(error)throw error;}, async retry(job,errorMessage,retryAfter){const terminal=job.attempts>=job.max_attempts;const {error}=await db.from("trend_style_research_jobs").update({status:terminal?"failed":"pending",error_message:errorMessage.slice(0,500),retry_after:terminal?null:retryAfter||null}).eq("id",job.id);if(error)throw error;} }; }

export function createConfiguredMarketInterestProvider():MarketInterestProvider { return createRepoOwnedMarketInterestProvider(); }
